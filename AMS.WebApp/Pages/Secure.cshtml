@page
@model AMS.WebApp.Pages.SecureModel
@{
}

<div class="d-flex flex-row">
    <h3>User: @Model.UserName</h3>
    <div class="flex-column">
        <button class="btn btn-info" onclick="">Logout</button>
    </div>
</div>
